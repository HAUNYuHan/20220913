
## 主動股票分析暨通知系統開發
```
關鍵技術
LINE BOT ==> 會員通知| 股票提醒
爬蟲 ==> 爬股票資料
資料分析 == > numpy, Matplotlib/seaborn, pandas
股票分析 ==> yfinance
Python 股票分析：Stocker 強大的股價預測分析工具 (附完整程式碼)
FinLab x Google雲端平台 | 3步驟實現全自動交易，從今以後躺著都能賺！(上)
python 入門到分析股市
進階主題
機器學習 + 人工智慧
雲端技術: k8s
```
### 安裝sdk
```
pip install line-bot-sdk==1.8.0
```
```
from flask import Flask
app = Flask(__name__)

from flask import Flask, request, abort
from linebot import LineBotApi, WebhookHandler
from linebot.exceptions import InvalidSignatureError
from linebot.models import MessageEvent, TextMessage, TextSendMessage
    
line_bot_api = LineBotApi('9/j057LM3uUD0lU5WK2bkNJQDC7g1e2He0sbNO3Y6muD6Tb8/FUU2teOmOU+Tc8uufr3RN2m0c0P2atyvKydBWpaNMd3k30z06iTgeKbxPQrGNFO28HRrIbht09jpPr+s7tq/sh6N7AHrPOMabwFswdB04t89/1O/w1cDnyilFU=')
handler = WebhookHandler('f319495ec680f2b4cb932efc3e4cca6c')

@app.route("/callback", methods=['POST'])
def callback():
    signature = request.headers['X-Line-Signature']
    body = request.get_data(as_text=True)
    try:
        habdler.handle(body, signature)
    except InvalidSignatureError:
        about(400)
    return 'Ok'
    
@handler.add(MessageEvent, message=TextMessage)
def handle_message(event):
    line_bot_api.reply_message(event.reply_token,
        TextSendMessage(text=event.message.text))
        
if __name__== '__main__':
    app.run()
            
```
```
C:\專題\linebot>python linebottest1.py
 * Serving Flask app "linebottest1" (lazy loading)
 * Environment: production
   WARNING: This is a development server. Do not use it in a production deployment.
   Use a production WSGI server instead.
 * Debug mode: off
 * Running on http://127.0.0.1:5000/ (Press CTRL+C to quit)
```
